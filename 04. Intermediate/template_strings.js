// Store this in template string ~
// The quick
// brown fox
// jumps over
// the lazy dog
let Text = `The quick
brown fox
jumps over
the lazy dog`;

console.log(`${Text}`);

// Create two variables (firstName, lastName)
let firstName = "James";
let lastName = "Leonard";

// Use that variables in template string.
console.log(`${firstName} ${lastName}`);
